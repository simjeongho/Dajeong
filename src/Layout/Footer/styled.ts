import styled from "styled-components";

export const LayoutFooter = styled.footer`
	width: 100%;
	height: 10rem;
	background: black;
	border-radius: 8px;
	position: relative;
	align-items: center;
	justify-content: center;
`;

export const LayoutFooterList = styled.ul`
	align-items: center;
	justify-content: center;
	margin: 1.5rem;
	height: 100%;
`;

export const LayoutFooterListItem = styled.li`
	list-style: none;
	font-family: var(--Font-googleSingleDay);
	color: var(--color-text-white);
`;
